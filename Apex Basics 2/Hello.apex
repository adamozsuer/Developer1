String quote='practice MAKES perfect';
system.debug(quote);
system.debug(quote.toUpperCAse());


String quote='practice MAKES perfect';
system.debug(quote);
system.debug(quote.toUpperCase());
system.debug(quote.capitalize());
system.debug(quote.length());
system.debug(quote.replace('practice', 'Remember, practice' ));
system.debug(quote.reverse());

